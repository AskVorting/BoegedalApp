package com.example.boegedalapp

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import com.example.boegedalapp.databinding.ActivityDetailedBinding
/*
class DetailedActivity : AppCompatActivity(){

    private lateinit var binding: ActivityDetailedBinding
    override fun onCreate(savedInstanceState: Bundle?){
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_detailed)

        val intent = this.intent
        if (intent != null)
        {
            val nameOfBeer = intent.getStringExtra("nameOfBeer")
            val typeOfBeer = intent.getStringExtra("typeOfBeer")
            val alcoholContent = intent.getStringExtra("alcoholContent")
            val image = intent.getIntExtra("image", R.drawable.beergyldenk)

            val price = intent.getStringExtra("price")

            binding.detailedNameOfBeer.text = nameOfBeer
            binding.detailedTypeOfBeer.text = typeOfBeer
            binding.detailedAlcoholContent.text = alcoholContent
            binding.detailedImage.setImageResource(image)

            binding.detailedPrice.text = price


        }

    }
}

 */